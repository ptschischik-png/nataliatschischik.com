{
  "name": "nataliatschischik-com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean-dist.mjs",
    "build": "npm run clean && eleventy && node scripts/extract-head-styles.mjs && node scripts/generate-sitemap.mjs",
    "check": "node scripts/check-site.mjs",
    "dev": "npm run clean && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
